spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/fis-kv
      aliases:
        hmc-topic-shared-access-key: AMQP_SHARED_ACCESS_KEY_VALUE
        hmc-fis-servicebus-connection-string: HMC_FIS_SERVICEBUS_CONNECTION_STRING
